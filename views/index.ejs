<!DOCTYPE html>
<html>
  <head>
    <title>Welcome to Contact Management System</title>
    <link href="/public/css/style.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
      body {
        background: linear-gradient(135deg, #e0eafc 0%, #cfdef3 100%);
        min-height: 100vh;
      }
      .welcome-container {
        max-width: 500px;
        margin: 80px auto 0 auto;
        background: #fff;
        border-radius: 18px;
        box-shadow: 0 8px 32px rgba(31,38,135,0.18);
        padding: 44px 32px 36px 32px;
      }
      h1 {
        color: #2563eb;
        font-weight: 700;
        margin-bottom: 1.5rem;
      }
      .lead {
        color: #374151;
        font-size: 1.2rem;
        margin-bottom: 2rem;
      }
      .btn-success {
        border-radius: 8px;
        font-weight: 600;
        font-size: 1.1rem;
        padding: 12px 32px;
        box-shadow: 0 2px 8px rgba(34,197,94,0.08);
        transition: background 0.2s, transform 0.15s;
      }
      .btn-success:hover, .btn-success:focus {
        background: #16a34a;
        transform: translateY(-2px) scale(1.03);
      }
      .btn-primary {
        border-radius: 8px;
        font-weight: 600;
        font-size: 1.1rem;
        padding: 12px 32px;
        box-shadow: 0 2px 8px rgba(37,99,235,0.08);
        transition: background 0.2s, transform 0.15s;
      }
      .btn-primary:hover, .btn-primary:focus {
        background: #1d4ed8;
        transform: translateY(-2px) scale(1.03);
      }
      @media (max-width: 600px) {
        .welcome-container {
          padding: 18px 6px;
        }
        h1 {
          font-size: 1.3rem;
        }
      }
    </style>
  </head>
  <body>
    <%- include('partials/header') %>
    <div class="welcome-container text-center">
      <h1 class="mb-4">Welcome to Contact Management System (CMS)</h1>
      <p class="lead">Securely manage your personal and business contacts with ease.</p>
      <div class="mt-4">
        <a href="/register" class="btn btn-success me-3">Register</a>
        <a href="/login" class="btn btn-primary">Login</a>
      </div>
    </div>
    <%- include('partials/footer') %>
  </body>
</html>